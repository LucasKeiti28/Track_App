import React from "react";
import { View, StyleSheet, Text } from "react-native";

const SignIn = () => {
  return <Text style={{ fontSize: 48 }}>SignIn Page</Text>;
};

const styles = StyleSheet.create({});

export default SignIn;
