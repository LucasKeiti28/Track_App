import React from "react";
import { View, StyleSheet, Text } from "react-native";

const Account = () => {
  return <Text style={{ fontSize: 48 }}>Account Page</Text>;
};

const styles = StyleSheet.create({});

export default Account;
